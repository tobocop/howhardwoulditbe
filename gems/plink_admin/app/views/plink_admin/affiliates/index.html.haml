= link_to 'Create New Affiliate', plink_admin.new_affiliate_path, class: 'button small'

%table.affiliate-list
  %thead
    %th ID
    %th Name
    %th Card Add Award Amount
    %th Join Award Amount
  %tbody
    - @affiliates.each do |affiliate|
      %tr.affiliate-item
        %td= affiliate.id
        %td= link_to affiliate.name, plink_admin.edit_affiliate_path(affiliate)
        %td= number_to_currency(affiliate.card_registration_dollar_award_amount)
        %td= number_to_currency(affiliate.join_dollar_award_amount)
