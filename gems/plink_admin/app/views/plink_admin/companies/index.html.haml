= link_to 'Create New Company', new_company_path, class: 'button small'
%table.company-list
  %thead
    %th Name
    %th Prospect
    %th Sales rep
    %th url
  %tbody
    - @companies.each do |company|
      %tr.company-item
        %td= link_to company.name, edit_company_path(company)
        %td= company.prospect? ? 'Yes' : 'No'
        %td= company.sales_rep.name
        %td= "https://analytics.plink.com/#{company.vanity_url}"

