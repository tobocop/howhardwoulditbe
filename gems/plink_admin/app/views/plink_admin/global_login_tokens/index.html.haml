= link_to 'Create New Global Login Token', new_global_login_token_path, class: 'button small'

%table.global_login_tokens-list
  %thead
    %th Edit
    %th Expires at
    %th Redirects to
    %th Token
    %th Link
  %tbody
    - @global_login_tokens.each do |global_login_token|
      %tr.global_login_token-item{class: global_login_token.expires_at < Time.zone.now ? 'expiring' : ''}
        %td= link_to 'Edit', edit_global_login_token_path(global_login_token)
        %td= global_login_token.expires_at
        %td= global_login_token.redirect_url
        %td= global_login_token.token
        %td= 'some_kind_of_link'

