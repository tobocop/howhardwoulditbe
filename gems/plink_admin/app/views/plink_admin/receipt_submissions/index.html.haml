%h1 Queues
%p please choose a queue to process submissions for
- Plink::ReceiptSubmissionRecord::QUEUE_RANGE.each do |i|
  - count = Plink::ReceiptSubmissionRecord.pending_by_queue(i).length
  = link_to "Process Queue #{i} (#{count} submissions)", plink_admin.receipt_submission_queue_path(i)
  %br
  %br
