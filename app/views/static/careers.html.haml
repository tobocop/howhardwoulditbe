#map-wrapper
  .feature#map-container
  %address
    %strong Plink HQ
    %br/
    2000 S. Colorado Blvd.
    %br/
    Suite 7000
    %br/
    Denver, CO 80222

.static.container

  .left-column
    %h1.font-3p2em.font-w900 Careers
    %h2.font-1p6em.font-w300.mbl You'll like the way we work it. No diggity.
    %h3.font-1p1em.font-w300.font-height1p5.mbxl
      Plink is a start-up made up of smart people who believe in each other. We're into things like agile development and ladder ball, and from time to time we ring an impressive looking gong.
      %br
      %br
      If you like our vibe and are passionate about your work, we'd like to hear from you.

  .right-column
    - career_items.each do |title, description|
      = render 'title_description_block', title: title, description: description

  .clear

= javascript_include_tag "https://maps.googleapis.com/maps/api/js?sensor=true"
:javascript
  $(document).ready(function() {
    var map;
      function initialize() {
        var location = new google.maps.LatLng(39.68025630, -104.93990410);

        var mapOptions = {
          zoom: 12,
          center: location,
          mapTypeId: google.maps.MapTypeId.ROADMAP
        };

        map = new google.maps.Map(document.getElementById('map-container'), mapOptions);

        var marker = new google.maps.Marker({
            position: location,
            map: map,
            title: 'Plink HQ'
        });
      }

      google.maps.event.addDomListener(window, 'load', initialize);
  });
