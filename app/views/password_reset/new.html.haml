.layout-inner

  %div{style: 'padding-top: 20px;'}

  %p Enter the email address associated with your account. We will send you an e-mail containing instructions for how you can reset your password.

  - if @password_reset.errors.any?
    - @password_reset.errors.full_messages.each do |message|
      %p= message

  = form_for(@password_reset, url: password_reset_path, as: 'password_reset') do |f|
    = f.text_field :email, placeholder: 'Email'
    = f.submit 'Send Password Reset Instructions', class: 'button primary_action'