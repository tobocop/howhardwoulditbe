- if @user_session.errors.any?
  - @user_session.errors.full_messages.each do |error|
    %p= error

%br
= form_for(@user_session, url: plink_session_path) do |f|
  = f.label :email
  = f.text_field :email
  = f.label :password
  = f.password_field :password
  = f.submit("Log in")
